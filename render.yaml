services:
  - type: web
    name: sail-material-management
    runtime: python
    plan: free
    buildCommand: npm --prefix frontend install && npm --prefix frontend run build && mkdir -p backend/static && cp -r frontend/dist/* backend/static/ && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: NODE_VERSION
        value: 20.12.0
      - key: ENVIRONMENT
        value: production
